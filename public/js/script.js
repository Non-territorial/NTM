function redirectToPayment(membershipType) {
    // This is a placeholder function. In a real application, you would
    // implement the logic to redirect to a payment gateway or process the payment.
    console.log(`Redirecting to payment for ${membershipType}`);
    alert(`You've selected ${membershipType}. Redirecting to payment...`);
    // Here you would typically redirect to a payment page or open a payment modal
    // window.location.href = `/payment?type=${membershipType}`;
}

